import { Typography } from '@code-judge/ui';

export const OR = () => {
  return (
    <Typography
      fontFamily={'roboto'}
      fontSize="body-s"
      className="text-body-secondary h-5 text-center w-full"
    >
      OR
    </Typography>
  );
};
