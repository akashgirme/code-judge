import { Typography } from '@code-judge/core-design';

export const OR = () => {
  return (
    <Typography
      fontFamily={'roboto'}
      fontSize="body-m"
      className="text-body-secondary  h-5 text-center w-full"
    >
      OR
    </Typography>
  );
};
