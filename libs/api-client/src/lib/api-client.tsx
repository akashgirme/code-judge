import styled from 'tailwind';

const StyledApiClient = styled.div`
  color: pink;
`;

export function ApiClient() {
  return (
    <StyledApiClient>
      <h1>Welcome to ApiClient!</h1>
    </StyledApiClient>
  );
}

export default ApiClient;
